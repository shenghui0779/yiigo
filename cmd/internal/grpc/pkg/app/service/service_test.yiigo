package service

import (
	"context"
	"testing"

	"{{.Module}}/pkg/{{.AppPkg}}/config"
)

func TestMain(m *testing.M) {
	// 初始化
	config.Init(context.Background(), "../config.toml")
	// 执行测试
	m.Run()
	// 关闭资源
	config.CleanUp()
}
